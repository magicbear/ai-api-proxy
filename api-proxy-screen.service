[Unit]
Description=OpenAI API Proxy Server in Screen Session
After=network.target

[Service]
Type=oneshot
RemainAfterExit=yes
User=your-user
Group=your-group
WorkingDirectory=/opt/api-proxy

# Start the server in a detached screen session named 'api-proxy'
ExecStart=/usr/bin/screen -dmS api-proxy /opt/api-proxy/start_in_screen.sh

# Stop the screen session when stopping the service
ExecStop=/usr/bin/screen -S api-proxy -X quit

# Environment settings
Environment=PATH=/home/your-user/.local/bin:/usr/local/bin:/usr/bin:/bin
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target